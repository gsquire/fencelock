language: c
dist: xenial
sudo: required
services:
    - docker
before_install:
    - docker-compose -f ci/docker-compose.yml build
    - docker-compose -f ci/docker-compose.yml -d up
script:
    - make
    - docker-compose -f ci/docker-compose.yml run test
notifications:
    email: false
